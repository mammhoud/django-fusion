# Active Undone Items

1. Resolve root settings DB configuration for migrate/populator path:
   - Current error: `settings.DATABASES ... Please supply the ENGINE value`.
2. Install/resolve internal package imports in active environment:
   - `django_grep`, `django_osoul`, `django_rseal`.
3. Run dev servers and HTTP page checks after dependency + DB config readiness.
4. Run alliance-focused test suite once `pytest` is available.

