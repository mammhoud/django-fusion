# ============================================================
# CTC Research Deployment - Single Site Only
# ============================================================
# Runs only CTC Research website with shared infrastructure
#
# Disabled sites:
#   - lms-demo/docker-compose.yml (commented out)
#   - VResume/docker-compose.yml (commented out)
# ============================================================

include:
  - path: compose/docker-compose.traefik.yml
  - path: compose/docker-compose.warehouse.yml
  - path: compose/docker-compose.yml
  - path: compose/docker-compose.tasks.yml
  - path: ctc-research/docker-compose.yml
  # - path: lms-demo/docker-compose.yml       # DISABLED
  # - path: VResume/docker-compose.yml        # DISABLED
  - path: compose/docker-compose.nginx.yml
