# ============================================================
# Alliance — Production Override
# Removes direct port, adds Traefik labels, tightens env.
# Usage: docker compose -f docker-compose.yml -f docker-compose.override.yml up -d
# ============================================================

services:
  core:
    ports: []
    environment:
      - SERVER_ENV=production
      - DEBUG=false
      - RELOAD=false
      - WORKERS=4
      - REDIS_URL=redis://:${REDIS_PASSWORD:-redis_password}@redis:6379/5
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-structa.cloud,core.structa.cloud}
    depends_on: []
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      - "traefik.http.services.alliance-service.loadbalancer.server.port=5080"
