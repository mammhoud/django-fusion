# pytest-core — run the full test suite with cwd set to projects/
#
# The workspace tests live at the repo root (tests/) and the django-fusion
# analyzer tests live under libs/django-fusion/tests/analyzer. Running
# pytest from projects/ uses projects/pyproject.toml, which points testpaths at both
# trees so the full suite is discovered.
name: pytest-core

on:
  pull_request:
    paths:
      - 'tests/**'
      - 'projects/**'
      - '.github/workflows/pytest-core.yml'
  push:
    branches: ['**']
    paths:
      - 'tests/**'
      - 'projects/**'
      - '.github/workflows/pytest-core.yml'
  workflow_dispatch:

jobs:
  pytest:
    name: Run pytest from projects/
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
          working-directory: projects
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up uv
        uses: astral-sh/setup-uv@v2
        with:
          version: 'latest'
          enable-cache: true

      - name: Sync dependencies
        run: uv sync

      - name: Run pytest
        run: uv run pytest
