# fusion-ci: Runs Django checks + tests, Astro builds + unit tests, and
# Django checks + tests, Astro builds + unit tests for precis-lms.
# Failures are isolated per job.
#
# Triggered on changes to fusion project files, lib submodules, or this
# workflow file.

name: Fusion CI (precis-lms)

on:
  pull_request:
    paths:
      - 'projects/precis/precis-lms/**'
      - 'libs/django-fusion/**'
      - 'libs/ceptor-ai/**'
      - 'libs/django-bolt/**'
      - 'applications/scripts/dev/check_domain_drift.sh'
      - '.github/workflows/fusion-ci.yml'
  push:
    branches: ['**']
    paths:
      - 'projects/precis/precis-lms/**'
      - 'libs/django-fusion/**'
      - 'libs/ceptor-ai/**'
      - 'libs/django-bolt/**'
      - '.github/workflows/fusion-ci.yml'
  workflow_dispatch:

# Prevent duplicate runs on the same PR/branch
concurrency:
  group: fusion-lms-ci-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  # ── CMS-Fusion Backend (Python/Django) ────────────────────────────────

  # ── LMS-Fusion Backend (Python/Django) ───────────────────────────────

  precis-lms-backend:
    name: LMS-Fusion — Django Checks + Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: projects
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Set up uv
        uses: astral-sh/setup-uv@v2
        with:
          version: 'latest'
          enable-cache: true

      - name: Sync dependencies
        run: uv sync

      - name: Run Django system checks
        run: make check WEBSITE=precis-lms

      - name: Verify root Makefile convenience target
        working-directory: ${{ github.workspace }}
        run: make precis-lms check

      - name: Run migrations
        working-directory: ${{ github.workspace }}
        run: make precis-lms migrate

      - name: Validate fixture files
        run: uv run python precis/precis-lms/backend/manage.py load_fusion_fixtures --full --dry-run

      - name: Run fixture content tests
        run: uv run python -m pytest precis/precis-lms/backend/tests/test_fixture_content.py -v --tb=short

      - name: Run remaining tests
        run: uv run python -m pytest precis/precis-lms/backend/tests/ --ignore=precis/precis-lms/backend/tests/test_fixture_content.py -q

  # ── LMS-Fusion Frontend (Astro) ─────────────────────────────────────

  precis-lms-frontend:
    name: LMS-Fusion — Build & Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: projects/precis/precis-lms/frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: projects/precis/precis-lms/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build Astro frontend
        run: npm run build

      - name: Run unit tests
        run: npm test

  # ── LMS-Fusion integration smoke (Astro + Django) ────────────────

  precis-lms-e2e:
    name: LMS-Fusion — Astro + Django Smoke
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      POSTGRES_PASSWORD: ci-postgres-password
      REDIS_PASSWORD: ci-redis-password
    defaults:
      run:
        working-directory: projects/precis/precis-lms/frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: projects/precis/precis-lms/frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Check Astro types
        run: npm run check

      - name: Build Astro frontend
        run: npm run build

      # spin up the backend so the homepage content tests can exercise the
      # full backend ↔ frontend pipeline (title/nav/hero/API consistency).
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create required Docker networks
        run: |
          for net in common traefik-net warehouse-net; do
            docker network inspect $net >/dev/null 2>&1 || docker network create $net
          done

      - name: Build precis-lms backend image
        run: |
          docker compose -f projects/precis/precis-lms/docker-compose.yml build \
            --build-arg PROJECT_PATH=precis-lms --build-arg SOURCE_PATH=precis/precis-lms backend

      - name: Start precis-lms backend (SQLite for CI E2E)
        run: |
          DB_TYPE=sqlite DB_HOST=127.0.0.1 \
            docker compose -f projects/precis/precis-lms/docker-compose.yml up -d --remove-orphans backend

      - name: Wait for precis-lms backend health
        run: |
          for i in $(seq 1 24); do
            if curl -sf http://127.0.0.1:5074/health/ >/dev/null 2>&1; then
              echo "✓ LMS-Fusion backend healthy"
              exit 0
            fi
            echo "  attempt $i/24 — waiting..."
            sleep 10
          done
          echo "✗ LMS-Fusion backend did not become healthy"
          docker compose -f projects/precis/precis-lms/docker-compose.yml logs --tail=50 backend
          exit 1

      # AI tests fetch homepage data from this URL. The fallback in
      # homepage-content.spec.ts handles the case where the backend is
      # unavailable (API-dependent assertions degrade gracefully).
      - name: Run Astro frontend smoke tests
        run: npm run test

      - name: Start Astro preview server
        run: |
          nohup npm run preview -- --host 127.0.0.1 --port 3001 > /tmp/precis-lms-astro-preview.log 2>&1 &
          echo $! > /tmp/precis-lms-astro-preview.pid
          for i in $(seq 1 30); do
            if curl -fsS http://127.0.0.1:3001/ >/dev/null; then
              echo "✓ LMS-Fusion frontend healthy"
              exit 0
            fi
            echo "  attempt $i/30 — waiting for frontend..."
            sleep 2
          done
          echo "✗ LMS-Fusion frontend did not become healthy"
          cat /tmp/precis-lms-astro-preview.log
          exit 1

      - name: Run LMS-Fusion Compose E2E tests
        env:
          E2E_BACKEND_URL: http://127.0.0.1:5074
          E2E_FRONTEND_URL: http://127.0.0.1:3001
        run: npm run test:e2e


  # ── Domain Drift Check ─────────────────────────────────────────────

  domain-drift-check:
    name: Domain Drift — precis-lms
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - uses: actions/checkout@v4

      - name: Check domain code for drift
        run: bash applications/scripts/dev/check_domain_drift.sh

  # ── Deploy to Staging (manual only, requires all checks to pass) ────

  deploy-staging:
    name: Deploy to Staging
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      POSTGRES_PASSWORD: ci-postgres-password
      REDIS_PASSWORD: ci-redis-password
    needs:
      - precis-lms-backend
      - precis-lms-frontend
      - precis-lms-e2e
      - domain-drift-check
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Create required Docker networks
        run: |
          for net in common traefik-net warehouse-net; do
            docker network inspect $net >/dev/null 2>&1 || docker network create $net
          done

      # ── Build & deploy LMS-Fusion ───────────────────────────────────

      - name: Build precis-lms backend image
        run: |
          docker compose -f projects/precis/precis-lms/docker-compose.yml build \
            --build-arg PROJECT_PATH=precis-lms --build-arg SOURCE_PATH=precis/precis-lms backend

      - name: Build precis-lms frontend image
        run: |
          docker compose -f projects/precis/precis-lms/docker-compose.yml build frontend

      - name: Start precis-lms services (SQLite for staging)
        run: |
          DB_TYPE=sqlite DB_HOST=127.0.0.1 \
            docker compose -f projects/precis/precis-lms/docker-compose.yml up -d --remove-orphans backend frontend

      - name: Wait for precis-lms backend health
        run: |
          for i in $$(seq 1 12); do
            if curl -sf http://127.0.0.1:5074/health/ >/dev/null 2>&1; then
              echo "✓ LMS-Fusion backend is healthy"
              exit 0
            fi
            echo "  attempt $$i/12 — waiting..."
            sleep 10
          done
          echo "✗ LMS-Fusion backend did not become healthy"
          echo "=== LMS-Fusion backend logs ==="
          docker compose -f projects/precis/precis-lms/docker-compose.yml logs --tail=50 backend
          exit 1

      - name: Show deployment status
        run: |
          echo "=== Docker Services Status ==="
          echo ""
          docker compose -f projects/precis/precis-lms/docker-compose.yml ps
          echo ""
          echo "=== Health Checks ==="
          curl -s http://127.0.0.1:5074/health/ && echo " — LMS-Fusion"
