# ── Node.js ──────────────────────────────────────────────────────────
node_modules/
.npm
.eslintcache
.next

# ── Python ───────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ── Environment & Secrets ────────────────────────────────────────────
.env
.env.*
!.env.example
*.sqlite3
# ── Generated / runtime ──────────────────────────────────────────────
logs/
*.log
test-results/
playwright-report/
*.tsbuildinfo
safe_loaddata.py
*.png
!projects/precis/precis-landing/backend/assets/static/related/formints/*.png
!projects/precis/precis-landing/frontend/public/static/related/formints/*.png
appmap.log
application/proxy/acme/acme-dns.json
application/databases/dockge-data/

# Legacy root-level media tree (runtime uploads; active media lives under projects/)
assets/media/

# Blinko runtime data (SurrealDB store + stale Postgres pre-migration data)
application/tools/blinko/blinko-data/
application/tools/blinko/blinko/blinko-data/

# Blinko build data: vendored build-context clone (nested git repo) and generated docker build context
application/tools/blinko/blinko/
application/tools/blinko/.blinko-build-context/

# Tools proxy basic-auth credentials (password hash)
application/tools/nginx/htpasswd

# Unreferenced root media artifact
ctc-research.logo.jpeg
# ── Docs artifacts & archives ────────────────────────────────────────
.archives/
archives/
backups/
.backups/
# ── macOS ────────────────────────────────────────────────────────────
.DS_Store

# ── Project-level patterns ───────────────────────────────────────────
libs/django-bolt
libs/ceptor-ai

.freebuff
.tmpprojects/formints/*.gif

# Databases and build artifacts (SQLite, Astro, Tauri/PyInstaller)
*.db
*.db-shm
*.db-wal
restaurant.db
.astro/
src-tauri/gen/
src-tauri/binaries/
sidecar/build/
*.pyz
*.toc
*.pkg
base_library.zip
**/src-tauri/gen/
**/src-tauri/binaries/
**/sidecar/build/

# AFFiNE and FileGator data are owned by the shared proxy stack.
# Keep this legacy pattern for workspaces that have not migrated yet.
.affine-data-*/
application/proxy/affine-data/
.hypothesis
.pnpm-store
.nx
.astro-doctor
*.sqlite3
db.sqlite3