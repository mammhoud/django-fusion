# Dependency Conflicts Report

Generated by `scripts/analyze_dependencies.py` — Phase 10: Dependency Alignment.

---

## Version Conflicts

The following packages are declared with different version constraints across
`pyproject.toml` files. The **Recommended** column shows the most restrictive
compatible constraint (highest lower bound).

### 1. `django`

| File | Constraint |
|------|-----------|
| `ctc-research.com/pyproject.toml` | *(unpinned)* |
| `structa.cloud/pyproject.toml` | *(unpinned)* |
| `venv/libs/django-osoul/pyproject.toml` | `>=4.0` |
| `venv/libs/django-rseal/pyproject.toml` | `>=4.0` |
| `venv/libs/django-grep/pyproject.toml` | *(unpinned)* |

**Recommended unified version:** `>=4.2`
*(Both projects require Python ≥3.11 and use Django 5.x features; >=4.2 is the
LTS baseline. The application projects pin via uv.lock so a lower bound is
sufficient for the packages.)*

---

### 2. `hypothesis`

| File | Constraint |
|------|-----------|
| `ctc-research.com/pyproject.toml` | `>=6.151.6` |
| `structa.cloud/pyproject.toml` | `>=6.151.6` |
| `venv/libs/nawaai/pyproject.toml` | `>=6.0` |

**Recommended unified version:** `>=6.151.6`
*(Use the highest lower bound declared by the application projects.)*

---

### 3. `pytest`

| File | Constraint |
|------|-----------|
| `structa.cloud/pyproject.toml` | `>=9.0.2` |
| `venv/libs/django-grep/pyproject.toml` | `>=7.0` |

**Recommended unified version:** `>=9.0.2`
*(Application projects already require 9.x; align the package to match.)*

---

### 4. `pytest-django`

| File | Constraint |
|------|-----------|
| `ctc-research.com/pyproject.toml` | `>=4.12.0` |
| `structa.cloud/pyproject.toml` | `>=4.12.0` |
| `venv/libs/django-grep/pyproject.toml` | `>=4.5` |

**Recommended unified version:** `>=4.12.0`
*(Use the highest lower bound declared by the application projects.)*

---

## Unused Dependencies (Conservative Analysis)

Only dependencies with **no matching import** anywhere in the source tree are
listed. Runtime-only packages (DB backends, WSGI servers, monitoring, etc.) are
excluded from this analysis.

### `ctc-research.com/pyproject.toml`

| Package | Finding |
|---------|---------|
| `environ` | Not imported; `django-environ` (separate package) is used instead via `import environ` in settings — **keep** if settings use `environ.Env()` |
| `fido2` | No import found in source tree — **candidate for removal** |
| `livereload` | No import found in source tree — **candidate for removal** |
| `marshmallow` | No import found in source tree — **candidate for removal** |
| `pyrefly` | No import found in source tree — **candidate for removal** |
| `pytest-django` | Used as a pytest plugin (no direct import needed) — **keep** |
| `pytest-selenium` | Used as a pytest plugin (no direct import needed) — **keep** |
| `python-json-logger` | Used as a string reference in `LOGGING` settings (`pythonjsonlogger.jsonlogger.JsonFormatter`) — **keep** |
| `stripe` | Stripe is used indirectly via `django_rseal.pipelines.services.payments` — **keep** |
| `twilio` | No import found in source tree — **candidate for removal** |
| `yml` | No import found in source tree — **candidate for removal** |

### `structa.cloud/pyproject.toml`

| Package | Finding |
|---------|---------|
| `pyrefly` | No import found in source tree — **candidate for removal** |
| `pytest-django` | Used as a pytest plugin — **keep** |
| `python-json-logger` | Used in `LOGGING` settings — **keep** |
| `yml` | No import found in source tree — **candidate for removal** |

### `venv/libs/django-grep/pyproject.toml`

| Package | Finding |
|---------|---------|
| `pytest-django` | Used as a pytest plugin — **keep** |

### `venv/libs/nawaai/pyproject.toml`

| Package | Finding |
|---------|---------|
| `toposort` | Not imported in nawaai source; used in `django-rseal` — **candidate for removal from nawaai** |

---

## Summary of Recommended Actions

### Version Unification (Task 10.2)

Apply these changes to align all `pyproject.toml` files:

| Package | Old (varied) | New (unified) |
|---------|-------------|---------------|
| `django` | unpinned / `>=4.0` | `>=4.2` |
| `hypothesis` | `>=6.0` / `>=6.151.6` | `>=6.151.6` |
| `pytest` | `>=7.0` / `>=9.0.2` | `>=9.0.2` |
| `pytest-django` | `>=4.5` / `>=4.12.0` | `>=4.12.0` |

### Dependency Removals (Task 10.3)

Conservative removals — only packages with no import evidence and no known
runtime-only usage:

| File | Package | Action |
|------|---------|--------|
| `ctc-research.com/pyproject.toml` | `fido2` | Remove |
| `ctc-research.com/pyproject.toml` | `livereload` | Remove |
| `ctc-research.com/pyproject.toml` | `marshmallow` | Remove |
| `ctc-research.com/pyproject.toml` | `pyrefly` | Remove |
| `ctc-research.com/pyproject.toml` | `twilio` | Remove |
| `ctc-research.com/pyproject.toml` | `yml` | Remove |
| `structa.cloud/pyproject.toml` | `pyrefly` | Remove |
| `structa.cloud/pyproject.toml` | `yml` | Remove |
| `venv/libs/nawaai/pyproject.toml` | `toposort` | Remove |

**Not removed (kept despite no direct import):**
- `environ` — may be used as `environ.Env()` in settings files
- `python-json-logger` — referenced as string in `LOGGING` config
- `stripe` — used transitively via `django_rseal`
- `pytest-django`, `pytest-selenium` — pytest plugins (no direct import)
- All runtime-only packages (DB backends, WSGI servers, etc.)
