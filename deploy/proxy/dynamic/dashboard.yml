http:
  routers:
    traefik-dashboard:
      rule: "Host(`traefik.structa.cloud`)"   # change to your subdomain
      entryPoints: [web-secure]
      service: api@internal
      middlewares: [basic-auth]
      tls:
        certResolver: letsencrypt
