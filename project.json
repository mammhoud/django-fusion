{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "django-fusion-assets",
  "root": "libs/django-fusion",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "npm run build", "cwd": "libs/django-fusion" } }
  }
}
