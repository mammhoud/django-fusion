# Root .dockerignore — applies when build context is workspace root
# ===============================================================
# Global patterns
# ===============================================================

# Dependencies (always rebuilt in Docker)
**/node_modules
**/.venv

# Python bytecode
**/__pycache__
**/*.pyc
**/*.pyo

# Build & cache artifacts
**/.sass-cache
**/.cache
**/dist
**/build
**/*.egg-info
**/.pytest_cache
**/.hypothesis
**/.ruff_cache
**/.mypy_cache
**/.tox
**/.nox

# OS files
**/.DS_Store
**/Thumbs.db

# Version control
**/.git
**/.gitignore
**/.gitattributes
**/.gitmodules

# IDE & editor config
**/.idea
**/.vscode
**/.claude

# Logs
**/*.log
**/logs

# Environment & secrets
**/.env
**/.env.*
**/*.key
**/*.pem
**/*.cert

# Backups
**/backups

# ===============================================================
# Tinker-specific exclusions
# ===============================================================

# Local SQLite database (re-created at runtime via entrypoint symlink)
tinker/db.sqlite3
tinker/data/

# Collected static files (rebuilt in Docker via collectstatic at build time)
tinker/staticfiles/
tinker/static_root/
tinker/media_root/

# Pre-built webpack bundles (rebuilt in Docker from source)
tinker/assets/bundles/
tinker/assets/node_modules/

# Source maps & workbox precache (generated by webpack, not needed at runtime)
tinker/assets/bundles/**/*.map
tinker/assets/bundles/**/precache-*

# ===============================================================
# Other site-specific exclusions (avoid sending unrelated data)
# ===============================================================
# The tinker build only copies tinker/ + libs/, but these
# patterns prevent accidental inclusion from the shared context.

# Other application directories (not copied by tinker Dockerfile)
ctc-research/assets/bundles/
lms-demo/assets/bundles/
VResume/assets/bundles/

# Shared workspace bundles
assets/bundles/
assets/media/
assets/staticfiles/
