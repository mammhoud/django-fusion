default:
  STRIPE_PUBLISHABLE_KEY: "@jinja {{ env.get('STRIPE_PUBLISHABLE_KEY', '') }}"
  STRIPE_SECRET_KEY: "@jinja {{ env.get('STRIPE_SECRET_KEY', '') }}"
  STRIPE_WEBHOOK_SECRET: "@jinja {{ env.get('STRIPE_WEBHOOK_SECRET', '') }}"
  PAYPAL_CLIENT_ID: "@jinja {{ env.get('PAYPAL_CLIENT_ID', '') }}"
  PAYPAL_CLIENT_SECRET: "@jinja {{ env.get('PAYPAL_CLIENT_SECRET', '') }}"

development:
  STRIPE_PUBLISHABLE_KEY: "@jinja {{ env.get('STRIPE_PUBLISHABLE_KEY', '') }}"
  STRIPE_SECRET_KEY: "@jinja {{ env.get('STRIPE_SECRET_KEY', '') }}"

production:
  STRIPE_PUBLISHABLE_KEY: "@jinja {{ env.get('STRIPE_PUBLISHABLE_KEY', '') }}"
  STRIPE_SECRET_KEY: "@jinja {{ env.get('STRIPE_SECRET_KEY', '') }}"
  STRIPE_WEBHOOK_SECRET: "@jinja {{ env.get('STRIPE_WEBHOOK_SECRET', '') }}"
  PAYPAL_CLIENT_ID: "@jinja {{ env.get('PAYPAL_CLIENT_ID', '') }}"
  PAYPAL_CLIENT_SECRET: "@jinja {{ env.get('PAYPAL_CLIENT_SECRET', '') }}"
