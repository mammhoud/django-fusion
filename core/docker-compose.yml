# ============================================================
# Main Orchestration — Website Services
# ============================================================
# Infrastructure Services (Traefik, PostgreSQL, Redis) are defined 
# in the root docker-compose.yml and automatically connected via shared networks.
#
# This file includes application containers that need rebuilding:
#   - ctc-research-website, lms-web, vresume-web, crm-website
# ============================================================

include:
  - path: ./lms-demo/docker-compose.yml
  - path: ./ctc-research/docker-compose.yml
  - path: ./VResume/docker-compose.yml
  # - path: ./crm/docker-compose.yml  # CRM directory is empty, not available
