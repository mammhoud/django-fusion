#!/bin/bash

# ==============================================================================
# 🐕 Django-RQ Worker Start Script
# ==============================================================================

set -o errexit
set -o pipefail
set -o nounset

# Load environment variables if needed
# source /app/.env

echo "Starting RQ worker..."
exec uv run python com rqworker default email newsletter
