"""Per-project environment overrides for Loop-CRM.

Site-specific YAML/env overrides for the Loop-CRM website. Unlike the Precis
sites, Loop-CRM resolves settings through ``os.environ`` rather than Dynaconf;
this file documents the site-level contract and keeps the ``Env`` layout in
parity with the other projects.
"""
