"""Fusion CMS REST API — shared REST endpoints (health, branding, layouts, urls).

The health/branding/layouts views live in django-fusion (``django_fusion.contrib.api``)
and are wired here; all other endpoints are app-specific API modules.
"""
