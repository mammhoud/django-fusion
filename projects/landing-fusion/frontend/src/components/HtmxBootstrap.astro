<!--
  HTMX bootstrap — loads the HTMX runtime and event listeners.
  Import this component on any page that uses LiveFragment or NewsletterSubscribe.
  Pages without HTMX payload no longer pay the ~61 KB cost.
-->

<script>
  import '../lib/htmx-bootstrap';
</script>
