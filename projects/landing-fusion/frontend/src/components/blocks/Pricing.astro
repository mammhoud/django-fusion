---
import PricingCard from '@/components/ui/PricingCard.astro';

export interface Plan {
  name: string;
  price: number;
  period: string;
  description: string;
  features: string[];
  highlighted?: boolean;
  cta: { label: string; href: string };
}

interface Props {
  heading?: string;
  intro?: string;
  plans?: Plan[];
  /** Render the pricing grid without its own <section> wrapper (used when folded into Features). */
  folded?: boolean;
}

const defaultPlans: Plan[] = [
  {
    name: 'Starter',
    price: 0,
    period: 'forever',
    description: 'For evaluating the stack on your own.',
    features: [
      'Static landing pages',
      'Community support',
      'Basic analytics',
      'Open-source license',
    ],
    cta: { label: 'Start free', href: '/#cta' },
  },
  {
    name: 'Pro',
    price: 29,
    period: 'per month',
    description: 'For teams shipping production sites.',
    highlighted: true,
    features: [
      'Wagtail page builder',
      'HTMX dynamic fragments',
      'Priority support',
      'Custom domains',
      'Team roles',
    ],
    cta: { label: 'Go Pro', href: '/#cta' },
  },
  {
    name: 'Enterprise',
    price: 99,
    period: 'per month',
    description: 'For organizations with real traffic.',
    features: [
      'Everything in Pro',
      'SSO & audit logs',
      'Dedicated success manager',
      'Custom branding',
      'API access',
    ],
    cta: { label: 'Contact sales', href: '/contact' },
  },
];

const {
  heading = 'Pricing, printed clearly',
  intro = 'No hidden fees, no per-seat math. Pick a sheet, ship a site.',
  plans = defaultPlans,
  folded = false,
} = Astro.props;
---

{folded ? (
  <div id="pricing" class="mt-20 border-t border-fu-line pt-16">
    <div class="mx-auto mb-14 max-w-2xl text-center">
      <p class="tag-marker mb-4">pricing</p>
      <h2 class="mb-4 font-display text-3xl font-bold tracking-tight text-fu-ink md:text-4xl">{heading}</h2>
      <p class="text-fu-muted">{intro}</p>
    </div>

    <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
      {plans.map((plan) => <PricingCard plan={plan} />)}
    </div>
  </div>
) : (
  <section id="pricing" class="doc-sheet bg-fu-paper py-20 md:py-24">
    <div class="container-fusion">
      <div class="mx-auto mb-14 max-w-2xl text-center">
        <p class="tag-marker mb-4">pricing</p>
        <h2 class="mb-4 font-display text-3xl font-bold tracking-tight text-fu-ink md:text-4xl">{heading}</h2>
        <p class="text-fu-muted">{intro}</p>
      </div>

      <div class="grid grid-cols-1 gap-6 md:grid-cols-3">
        {plans.map((plan) => <PricingCard plan={plan} />)}
      </div>
    </div>
  </section>
)}
