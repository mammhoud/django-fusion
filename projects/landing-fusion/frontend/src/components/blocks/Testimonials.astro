---
interface Testimonial {
  quote: string;
  author: string;
  role: string;
}

interface Props {
  heading?: string;
  intro?: string;
  testimonials?: Testimonial[];
}

const defaultTestimonials: Testimonial[] = [
  {
    quote:
      'We swapped a 480 KB React bundle for server-rendered HTML and our load time halved. The page is the document — nothing in between.',
    author: 'Sarah Mitchell',
    role: 'CTO, EduStart',
  },
  {
    quote:
      'Alpine handled the accordion and modals in an afternoon. We deleted more state-management code than we wrote this quarter.',
    author: 'David Chen',
    role: 'Lead Developer, LearnLoop',
  },
  {
    quote:
      'Search engines read our finished pages instead of a JS shell. Organic traffic doubled within a month, with zero extra work.',
    author: 'Amira Hassan',
    role: 'Marketing Director, SkillBridge',
  },
];

const {
  heading = 'The stack, reviewed in production',
  intro = 'Teams that moved off heavy SPAs, in their own words.',
  testimonials = defaultTestimonials,
} = Astro.props;
---

<section class="doc-sheet bg-fu-paper py-20 md:py-24">
  <div class="container-fusion">
    <div class="mx-auto mb-14 max-w-2xl text-center">
      <p class="tag-marker mb-4">reviews</p>
      <h2 class="mb-4 font-display text-3xl font-bold tracking-tight text-fu-ink md:text-4xl">{heading}</h2>
      <p class="text-fu-muted">{intro}</p>
    </div>

    <!-- Alpine carousel -->
    <div class="mx-auto max-w-3xl" x-data={`{ active: 0, total: ${testimonials.length} }`}>
      <div class="overflow-hidden">
        <div class="flex transition-transform duration-500 ease-out" :style="`transform: translateX(-${active * 100}%)`">
          {testimonials.map((t) => (
            <div class="w-full flex-shrink-0 px-1">
              <figure class="card relative p-8 md:p-10">
                <span class="absolute left-6 top-5 font-mono text-[0.68rem] uppercase tracking-[0.14em] text-fu-muted/70" aria-hidden="true">
                  {String(testimonials.indexOf(t) + 1).padStart(2, '0')} / {String(testimonials.length).padStart(2, '0')}
                </span>

                <!-- Rating as a file-inspect line, not stars -->
                <div class="mb-5 flex items-center gap-2 font-mono text-[0.72rem] uppercase tracking-[0.16em] text-fu-link" aria-label="Rated 5 out of 5">
                  <svg class="h-3.5 w-3.5" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                    <path d="M10 15l-5.9 3.1 1.1-6.6L.5 6.9l6.6-1L10 0l2.9 5.9 6.6 1-4.7 4.6 1.1 6.6z" />
                  </svg>
                  5.0 / 5.0
                </div>

                <blockquote class="mb-6 font-display text-lg leading-relaxed text-fu-ink/85">
                  &ldquo;{t.quote}&rdquo;
                </blockquote>

                <figcaption class="flex items-center gap-4 border-t border-fu-line pt-5">
                  <div class="flex h-11 w-11 items-center justify-center rounded-full border border-fu-line bg-fu-paper font-mono text-sm font-semibold text-fu-link">
                    {t.author.split(' ').map((n) => n[0]).join('')}
                  </div>
                  <div>
                    <div class="font-medium text-fu-ink">{t.author}</div>
                    <div class="font-mono text-[0.72rem] uppercase tracking-wider text-fu-muted">{t.role}</div>
                  </div>
                </figcaption>
              </figure>
            </div>
          ))}
        </div>
      </div>

      <!-- Controls -->
      <div class="mt-7 flex items-center justify-center gap-4">
        <button
          type="button"
          class="btn-secondary !h-10 !w-10 !p-0"
          @click="active = active > 0 ? active - 1 : total - 1"
          aria-label="Previous testimonial"
        >
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/>
          </svg>
        </button>
        <div class="flex gap-2">
          {testimonials.map((testimonial, i) => (
            <button
              type="button"
              class="h-1.5 rounded-full transition-all duration-300"
              :class={`active === ${i} ? 'w-7 bg-fu-link' : 'w-4 bg-fu-line hover:bg-fu-link/50'`}
              @click={`active = ${i}`}
              :aria-label={`Go to testimonial ${i + 1}`}
              title={testimonial.author}
            ></button>
          ))}
        </div>
        <button
          type="button"
          class="btn-secondary !h-10 !w-10 !p-0"
          @click="active = active < total - 1 ? active + 1 : 0"
          aria-label="Next testimonial"
        >
          <svg class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
          </svg>
        </button>
      </div>
    </div>
  </div>
</section>
