---
import Layout from '@/layouts/Layout.astro';
import Hero from '@/components/blocks/Hero.astro';
import Testimonials from '@/components/blocks/Testimonials.astro';
import CTA from '@/components/blocks/CTA.astro';
import Toast from '@/components/ui/Toast.astro';
import RenderModeSwitch from '@/components/ui/RenderModeSwitch.astro';
import ContentError from '@/components/ui/ContentError.astro';
import HtmxBootstrap from '@/components/HtmxBootstrap.astro';
import { cachedSiteSettings, cachedNavigation, cachedPageData } from '@/lib/api';
import type { SiteSettings, NavigationData, PageData } from '@/lib/api';

let siteSettings: SiteSettings | undefined;
let navData: NavigationData | undefined;
let pageData: PageData | undefined;
try {
  [siteSettings, navData, pageData] = await Promise.all([
    cachedSiteSettings(), cachedNavigation(), cachedPageData('features'),
  ]);
} catch { /* use fallbacks */ }

const pageHero = pageData?.hero;
const pageCta = pageData?.cta;
const pageFeatures = (pageData?.features as any[] | undefined);
const pageTestimonials = (pageData?.testimonials as any[] | undefined);

const capabilities = pageFeatures?.map((f: any) => ({
  title: String(f.title || ''),
  text: String(f.description || ''),
  icon: String(f.icon || 'M13 10V3L4 14h7v7l9-11h-7z'),
}));
---

<Layout title={pageData?.title || 'Benefits'} description={pageData?.search_description || 'The practical benefits of applications that make customer journeys clearer and everyday work easier.'} navItems={navData?.nav_items} siteSettings={siteSettings} pageData={pageData} pageSlug="features">
  <Hero eyebrow={pageHero?.badge || 'benefits · the reason to switch'} heading={pageHero?.title || 'Built for better outcomes'} accent='outcomes' intro={pageHero?.subtitle || 'Choose applications that help customers move forward and give your team more time, clarity, and control.'} primaryCta={{ label: 'See the products', href: '/products/' }} secondaryCta={{ label: 'Talk to us', href: '/contact/' }} status='200 OK · text/html · benefits in one document' />

  {pageData?.body ? (
    <section class='doc-sheet bg-fu-card py-16 md:py-20'>
      <div class='container-fusion'>
        <div class='prose-fusion max-w-3xl' set:html={pageData.body}></div>
      </div>
    </section>
  ) : null}

  {capabilities?.length ? (
  <section class='doc-sheet bg-fu-paper py-20 md:py-24'>
    <div class='container-fusion'>
      <div class='mx-auto mb-14 max-w-2xl text-center'>
        <p class='tag-marker mb-4'>benefits of switching</p>
        <h2 class='mb-4 font-display text-3xl font-bold tracking-tight text-fu-ink md:text-4xl'>Benefits of switching to our applications</h2>
        <p class='text-fu-muted'>Make important work easier to start, easier to understand, and easier for your team to keep improving.</p>
      </div>
      <div class='grid grid-cols-1 gap-5 md:grid-cols-2 lg:grid-cols-3'>
        {capabilities!.map((cap, i) => (
          <div class='card reveal p-6' x-data x-intersect='`$el.classList.add("reveal-visible")`' style={`--reveal-i: ${i}`}>
            <div class='mb-4 inline-flex h-10 w-10 items-center justify-center rounded-md border border-fu-line bg-fu-card text-fu-link'>
              <svg class='h-5 w-5' fill='none' viewBox='0 0 24 24' stroke='currentColor' stroke-width='1.8' aria-hidden='true'><path stroke-linecap='round' stroke-linejoin='round' d={cap.icon} /></svg>
            </div>
            <h3 class='mb-2 font-display text-[1.05rem] font-semibold text-fu-ink'>{cap.title}</h3>
            <p class='text-sm leading-relaxed text-fu-muted'>{cap.text}</p>
          </div>
        ))}
      </div>
    </div>
  </section>
  ) : <ContentError label="Capabilities data unavailable" />}

  {pageTestimonials?.length ? <Testimonials testimonials={pageTestimonials.map((t: any) => ({ quote: String(t.quote || ''), author: String(t.author || ''), role: String(t.role || '') }))} /> : null}
  <RenderModeSwitch route='/features/' />
  <CTA heading={pageCta?.title || 'Switch to work that feels lighter'} intro={pageCta?.subtitle || 'Bring one customer or team workflow that feels harder than it should. We will help you find a useful first step.'} ctaLabel={pageCta?.primary_cta?.label || 'Explore products'} ctaHref={pageCta?.primary_cta?.href || '/products/'} />
  <Toast />
  <HtmxBootstrap />
</Layout>
