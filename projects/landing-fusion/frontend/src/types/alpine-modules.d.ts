// Ambient module declarations for Alpine.js packages (no bundled types).
// This file must stay a "global script" (no imports/exports) so the
// `declare module` statements apply project-wide.
declare module 'alpinejs';
declare module '@alpinejs/collapse';
declare module '@alpinejs/intersect';
