---
/**
 * About — minimal shell. The backend fragment (/fragment/pages/about/) is the
 * single source of truth for the whole document (hero, sub-nav, services
 * brief, mission, stats, features+pricing, testimonials, faq, cta).
 */
import BackendPage from '@/components/BackendPage.astro';
---

<BackendPage slug="about" />
