[project]
name = "cms-full"
version = "1.0.0"
description = "Unified CMS Full (CTC Research + LMS) — part of Structa Cloud workspace"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
