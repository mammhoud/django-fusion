# POS Solo Sidecar dependencies
# Synchronised with pyproject.toml — use `uv sync` or `pip install -r requirements.txt`

robyn>=0.50,<1.0
django>=4.2,<5.1
asgiref>=3.8
httpx>=0.27
pydantic>=2.0

# Test dependencies
pytest>=7.0
pytest-asyncio>=0.23
Jinja2>=3.1.0
