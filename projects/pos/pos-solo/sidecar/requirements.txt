# POS Full Sidecar dependencies
# ⚠️  This file is a reference only. All deps are in pyproject.toml.
# Use `uv sync` (from sidecar/) to install — this creates a .venv and
# generates sidecar/uv.lock. See Makefile `make server` / `make server-dev`.
#
#   cd sidecar && uv sync
#   uv run python3 server.py --port 8766
#
# Historical pip install:
#   pip install -r requirements.txt  (deprecated — prefer uv sync)
