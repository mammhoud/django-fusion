/**
 * POS API (Minimal Edition)
 * ============================
 * Local-only API layer. No sidecar server required.
 *
 * This edition uses Tauri invoke() for all backend operations
 * via the Rust/SQLite layer. No external API dependencies.
 */

export {};
