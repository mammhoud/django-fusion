[project]
name = "lms-fusion"
version = "1.0.3"
description = "Structa Cloud LMS Fusion website backend"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
