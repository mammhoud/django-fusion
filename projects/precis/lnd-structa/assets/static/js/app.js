/**
 * Landing-Fusion — Django-side JavaScript entry point.
 * Bundled by webpack (webpack/landing-fusion.config.js).
 * Consumed by django-webpack-loader via {% render_bundle 'landing' 'js' %}.
 */

// HTMX is loaded separately via <script> tag — this file is for
// project-specific JS modules and Alpine.js component initialization.

console.log('[landing-fusion] assets loaded');
