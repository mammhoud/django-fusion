[project]
name = "ctc-research"
version = "1.0.3"
description = "CTC Research medical research center website backend"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]
