[pytest]
# Keep the nested test configuration aligned with backend/pytest.ini.
# The explicit environment override in the Makefile also wins over a
# production container's DJANGO_SETTINGS_MODULE value.
DJANGO_SETTINGS_MODULE = tests.test_settings
