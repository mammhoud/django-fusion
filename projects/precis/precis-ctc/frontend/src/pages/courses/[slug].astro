---
import Layout from '@/layouts/Layout.astro';
import CTA from '@/components/blocks/CTA.astro';
import ContentError from '@/components/ui/ContentError.astro';
import HtmxBootstrap from '@/components/HtmxBootstrap.astro';
import { fetchCourseDetail, fetchCourseList } from '@/lib/api';
import { cachedSiteSettings, cachedNavigation } from '@/lib/api';
import type { CourseDetail, NavigationData, SiteSettings } from '@/lib/api';

export async function getStaticPaths() {
  // Build-time content is optional: Docker/CI can build the static shell
  // before Django is running. The backend still supplies the authoritative
  // list at runtime; unknown slugs fall through to the ContentError branch.
  try {
    const catalog = await fetchCourseList();
    if (catalog.data.length) return catalog.data.map((course) => ({ params: { slug: course.slug } }));
  } catch {
    // Backend unreachable during docker build — emit empty path set.
  }
  return [];
}

const { slug } = Astro.params;
let siteSettings: SiteSettings | undefined;
let navData: NavigationData | undefined;
try {
  [siteSettings, navData] = await Promise.all([
    cachedSiteSettings(),
    cachedNavigation(),
  ]);
} catch {
  // Site chrome may be unavailable during a preview build.
}
let course: CourseDetail | undefined;
try {
    course = await fetchCourseDetail(slug!);
  } catch {
    // Unknown slugs still render the normal ContentError fallback.
  }

const formatDifficulty = (value: string) => value.replace(/\b\w/g, (char) => char.toUpperCase());
const overviewBlocks = Array.isArray(course?.overview) ? course.overview : [];
const objectives = Array.isArray(course?.objectives) ? course.objectives : [];
const targetAudience = Array.isArray(course?.target_audience) ? course.target_audience : [];
const requirements = Array.isArray(course?.requirements) ? course.requirements : [];
const specializations = Array.isArray(course?.specializations) ? course.specializations : [];
const tags = Array.isArray(course?.tags) ? course.tags : [];
const modules = Array.isArray(course?.modules) ? course.modules : [];
const previewVideo = Array.isArray(course?.preview_video_url) ? course.preview_video_url : (course?.preview_video_url ? [String(course.preview_video_url)] : []);
const heroImage = course?.image_url || course?.header_image;
---

<Layout
  title={course?.title || 'Course Detail'}
  description={course?.short_description || 'Course detail page.'}
  navItems={navData?.nav_items}
  siteSettings={siteSettings}
  pageSlug={`course:${slug}`}
>
  {course ? (
    <>
      <!-- ===== HERO — Cinematic Full-Bleed ===== -->
      <section class="course-hero" style={heroImage ? `--hero-image: url('${heroImage}')` : ''}>
        <div class="course-hero__overlay"></div>
        <div class="course-hero__inner">
          <div class="course-hero__content reveal-up">
            <span class="course-hero__eyebrow">{formatDifficulty(course.difficulty)} · {course.duration}h · {course.language.toUpperCase()}</span>
            <h1 class="course-hero__title">{course.title}</h1>
            <p class="course-hero__subtitle">{course.short_description}</p>
            <div class="course-hero__actions">
              <a href="#enroll" class="btn-magnetic">
                <span>Enroll now</span>
                <span class="btn-magnetic__icon">
                  <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true"><path d="M1 13L13 1M13 1H4.5M13 1V9.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                </span>
              </a>
              <a href="/courses/" class="btn-ghost">
                <span>← Back to catalog</span>
              </a>
            </div>
          </div>
          <!-- Stats strip -->
          <div class="course-hero__stats reveal-up" style="--reveal-delay: 0.15s">
            {course.enrolled_count ? <div class="course-hero__stat"><span class="course-hero__stat-value">{course.enrolled_count}+</span><span class="course-hero__stat-label">Students</span></div> : null}
            <div class="course-hero__stat"><span class="course-hero__stat-value">{modules.length}</span><span class="course-hero__stat-label">Modules</span></div>
            <div class="course-hero__stat"><span class="course-hero__stat-value">{course.duration}h</span><span class="course-hero__stat-label">Duration</span></div>
            {course.has_certificate ? <div class="course-hero__stat"><span class="course-hero__stat-value">✓</span><span class="course-hero__stat-label">Certificate</span></div> : null}
          </div>
        </div>
      </section>

      <!-- ===== MAIN CONTENT — Asymmetrical Bento ===== -->
      <section class="course-main">
        <div class="course-main__grid">

          <!-- LEFT: Content column -->
          <div class="course-main__content">

            <!-- WHY THIS COURSE — Double-Bezel card -->
            <article class="card-premium reveal-up">
              <div class="card-premium__shell">
                <div class="card-premium__core">
                  <p class="tag-marker">course overview</p>
                  <h2 class="card-premium__title">Why this course matters</h2>
                  <p class="card-premium__body">{course.description}</p>
                  {heroImage && !heroImage.includes('placeholder') ? <img src={heroImage} alt={course.title} loading="lazy" class="card-premium__hero-image" /> : null}
                </div>
              </div>
            </article>

            <!-- OVERVIEW BLOCKS -->
            {overviewBlocks.length ? (
              <article class="card-premium reveal-up" style="--reveal-delay: 0.1s">
                <div class="card-premium__shell">
                  <div class="card-premium__core">
                    <div class="card-premium__blocks">
                      {overviewBlocks.map((block: any) => (
                        <div class="card-premium__block">
                          {block.value?.heading && <h3 class="card-premium__block-title">{block.value.heading}</h3>}
                          {block.value?.body && <p class="card-premium__body-sm">{String(block.value.body)}</p>}
                          {Array.isArray(block.value?.list_items) && <ul class="card-premium__list">{block.value.list_items.map((item: string) => <li><span>✓</span><span>{item}</span></li>)}</ul>}
                        </div>
                      ))}
                    </div>
                  </div>
                </div>
              </article>
            ) : null}

            <!-- LEARNING OBJECTIVES -->
            {objectives.length ? (
              <article class="card-premium reveal-up" style="--reveal-delay: 0.15s">
                <div class="card-premium__shell">
                  <div class="card-premium__core">
                    <h3 class="card-premium__section-title">What you'll learn</h3>
                    <ul class="card-premium__objectives-grid">
                      {objectives.map((item: string) => <li><span class="check-icon">✓</span><span>{item}</span></li>)}
                    </ul>
                  </div>
                </div>
              </article>
            ) : null}

            <!-- WHO IT IS FOR -->
            {targetAudience.length ? (
              <article class="card-premium reveal-up" style="--reveal-delay: 0.2s">
                <div class="card-premium__shell">
                  <div class="card-premium__core">
                    <h3 class="card-premium__section-title">Who this course is for</h3>
                    <ul class="card-premium__list">
                      {targetAudience.map((item: string) => <li><span>→</span><span>{item}</span></li>)}
                    </ul>
                  </div>
                </div>
              </article>
            ) : null}

            <!-- REQUIREMENTS -->
            {requirements.length ? (
              <article class="card-premium reveal-up" style="--reveal-delay: 0.25s">
                <div class="card-premium__shell">
                  <div class="card-premium__core">
                    <h3 class="card-premium__section-title">Requirements</h3>
                    <ul class="card-premium__list">
                      {requirements.map((item: string) => <li><span>•</span><span>{item}</span></li>)}
                    </ul>
                  </div>
                </div>
              </article>
            ) : null}

            <!-- TAGS -->
            {(specializations.length || tags.length) ? (
              <div class="flex flex-wrap gap-2 mt-6 reveal-up" style="--reveal-delay: 0.3s">
                {[...specializations, ...tags].map((label: string) => <span class="badge-primary">{label}</span>)}
              </div>
            ) : null}

            <!-- PREVIEW VIDEOS -->
            {previewVideo.length ? (
              <article class="card-premium reveal-up" style="--reveal-delay: 0.35s">
                <div class="card-premium__shell">
                  <div class="card-premium__core">
                    <h3 class="card-premium__section-title">Course preview</h3>
                    <div class="card-premium__video-links">
                      {previewVideo.map((url: string) => (
                        <a href={url} target="_blank" rel="noreferrer" class="btn-secondary inline-flex">Watch preview →</a>
                      ))}
                    </div>
                  </div>
                </div>
              </article>
            ) : null}

          </div>

          <!-- RIGHT: Sticky sidebar — Double-Bezel pricing card -->
          <aside class="course-main__sidebar" id="enroll">
            <div class="card-premium card-premium--sticky reveal-up" style="--reveal-delay: 0.05s">
              <div class="card-premium__shell">
                <div class="card-premium__core">
                  <p class="tag-marker mb-4">course facts</p>

                  <!-- Price -->
                  <div class="sidebar-price">
                    {course.is_free
                      ? <span class="sidebar-price__value text-success">Free</span>
                      : course.is_discounted
                        ? <>
                            <span class="sidebar-price__value">${course.current_price}</span>
                            <span class="sidebar-price__original">${course.original_price}</span>
                            <span class="sidebar-price__discount">-{course.discount_percentage_calculated}%</span>
                          </>
                        : <span class="sidebar-price__value">${course.price}</span>
                    }
                  </div>

                  <!-- Facts -->
                  <dl class="sidebar-facts">
                    <div class="sidebar-facts__row"><dt>Duration</dt><dd>{course.duration} hours</dd></div>
                    <div class="sidebar-facts__row"><dt>Level</dt><dd>{formatDifficulty(course.difficulty)}</dd></div>
                    <div class="sidebar-facts__row"><dt>Language</dt><dd class="uppercase">{course.language}</dd></div>
                    <div class="sidebar-facts__row"><dt>Modules</dt><dd>{modules.length}</dd></div>
                    <div class="sidebar-facts__row"><dt>Certificate</dt><dd>{course.has_certificate ? 'Included' : '—'}</dd></div>
                    {course.average_rating ? <div class="sidebar-facts__row"><dt>Rating</dt><dd class="sidebar-facts__rating">{'★'.repeat(Math.round(course.average_rating))} {course.average_rating}</dd></div> : null}
                  </dl>

                  <!-- CTA -->
                  <a href="/contact/" class="btn-magnetic btn-magnetic--full mt-6">
                    <span>{course.is_free ? 'Enroll for free' : 'Enroll now'}</span>
                    <span class="btn-magnetic__icon">
                      <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true"><path d="M1 13L13 1M13 1H4.5M13 1V9.5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                    </span>
                  </a>

                  {course.is_discounted && course.discount_until ? (
                    <p class="sidebar-discount-note">Offer ends {new Date(course.discount_until).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}</p>
                  ) : null}

                  {course.has_certificate ? (
                    <div class="sidebar-cert-badge">
                      <svg width="20" height="20" viewBox="0 0 20 20" fill="none" aria-hidden="true"><circle cx="10" cy="9" r="6" stroke="#22c55e" stroke-width="1.5"/><path d="M7 9l2 2 4-4" stroke="#22c55e" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/><path d="M6 14l1.5 3h5L14 14" stroke="#22c55e" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/></svg>
                      <span>Certificate included</span>
                    </div>
                  ) : null}
                </div>
              </div>
            </div>
          </aside>

        </div>
      </section>

      <!-- ===== SYLLABUS — Smooth Accordion ===== -->
      {modules.length ? (
        <section class="syllabus-section reveal-up">
          <div class="syllabus-section__inner">
            <div class="syllabus-section__header">
              <p class="tag-marker">learning path</p>
              <h2 class="syllabus-section__title">Course syllabus</h2>
              <p class="syllabus-section__subtitle">{modules.length} modules · step-by-step learning path</p>
            </div>

            <div class="syllabus-accordion" id="syllabus">
              {modules.map((mod: any, idx: number) => (
                <div class="syllabus-accordion__item">                    <button
                    class="syllabus-accordion__trigger"
                    aria-expanded={idx === 0 ? 'true' : 'false'}
                  >
                    <span class="syllabus-accordion__index">{String(idx + 1).padStart(2, '0')}</span>
                    <span class="syllabus-accordion__label">
                      <span class="syllabus-accordion__module-title">{mod.title}</span>
                      {mod.description && <span class="syllabus-accordion__module-desc">{mod.description}</span>}
                    </span>
                    <span class="syllabus-accordion__meta">
                      <span class="syllabus-accordion__lesson-count">{mod.lessons?.length || 0} lessons</span>
                      <span class="syllabus-accordion__chevron">
                        <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><path d="M4 6l4 4 4-4" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/></svg>
                      </span>
                    </span>
                  </button>
                  <div class="syllabus-accordion__panel">
                    {mod.lessons?.length ? (
                      <ul class="syllabus-accordion__lessons">
                        {mod.lessons.map((lesson: any) => (
                          <li class="syllabus-accordion__lesson">
                            <span class="syllabus-accordion__lesson-icon">
                              {lesson.is_preview
                                ? <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><circle cx="8" cy="8" r="7" stroke="#22c55e" stroke-width="1.5"/><path d="M6.5 5.5l4 2.5-4 2.5V5.5z" fill="#22c55e" stroke="#22c55e" stroke-width="0.5"/></svg>
                                : <svg width="16" height="16" viewBox="0 0 16 16" fill="none" aria-hidden="true"><circle cx="8" cy="8" r="7" stroke="currentColor" stroke-width="1.2" opacity="0.35"/><path d="M6.5 5.5l4 2.5-4 2.5V5.5z" fill="currentColor" opacity="0.35"/></svg>
                              }
                            </span>
                            <span class="syllabus-accordion__lesson-title">{lesson.title}</span>
                            <span class="syllabus-accordion__lesson-duration">{lesson.duration || 0} min</span>
                            {lesson.is_preview ? <span class="syllabus-accordion__lesson-badge">Preview</span> : null}
                          </li>
                        ))}
                      </ul>
                    ) : null}
                  </div>
                </div>
              ))}
            </div>
          </div>
        </section>
      ) : null}

      <!-- ===== INSTRUCTOR — Editorial Card ===== -->
      {course.instructor ? (
        <section class="instructor-section reveal-up">
          <div class="instructor-section__inner">
            <div class="card-premium">
              <div class="card-premium__shell">
                <div class="card-premium__core instructor-card">
                  {course.instructor?.profile?.avatar ? (
                    <img src={course.instructor.profile.avatar} alt={course.instructor.name || course.instructor.username} class="instructor-card__avatar" width="100" height="100" loading="lazy" />
                  ) : (
                    <div class="instructor-card__avatar-placeholder">{course.instructor.name?.[0] || 'I'}</div>
                  )}
                  <div class="instructor-card__info">
                    <p class="tag-marker">instructor</p>
                    <h3 class="instructor-card__name">{course.instructor.name || course.instructor.username}</h3>
                    {course.instructor?.profile?.title && <p class="instructor-card__title">{course.instructor.profile.title}</p>}
                    {course.instructor?.profile?.bio && <p class="instructor-card__bio">{course.instructor.profile.bio}</p>}
                    <div class="instructor-card__links">
                      {course.instructor?.profile?.website ? <a href={course.instructor.profile.website} class="btn-ghost-sm">Website</a> : null}
                      {course.instructor?.profile?.linkedin ? <a href={course.instructor.profile.linkedin} class="btn-ghost-sm">LinkedIn</a> : null}
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </section>
      ) : null}

      <!-- ===== CTA ===== -->
      <CTA
        heading={course?.has_certificate ? 'Earn your certificate today' : 'Ready to start learning?'}
        intro={course?.has_certificate ? 'Complete this course and earn a verifiable certificate. Join students already enrolled.' : 'Join the students already advancing their careers with this course.'}
        ctaLabel="Return to catalog"
        ctaHref="/courses/"
      />
    </>
  ) : (
    <section class="py-32"><div class="container"><ContentError label="Course not found" showHomeLink={true} /></div></section>
  )}
  <HtmxBootstrap />
</Layout>

<!-- ===== REVEAL ANIMATIONS + ACCORDION ===== -->
<script>
  // Staggered reveal on scroll
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry) => {
      if (entry.isIntersecting) {
        entry.target.classList.add('reveal-up--visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.15, rootMargin: '0px 0px -40px 0px' });

  const revealTargets = document.querySelectorAll('.reveal-up');
  if (!('IntersectionObserver' in window)) {
    revealTargets.forEach((el) => el.classList.add('reveal-up--visible'));
  } else {
    revealTargets.forEach((el) => observer.observe(el));
  }

  // Accordion toggle (delegated — CSP-safe, no inline onclick)
  document.getElementById('syllabus')?.addEventListener('click', (e) => {
    const target = e.target as Element | null;
    const trigger = target?.closest('.syllabus-accordion__trigger') as HTMLElement | null;
    if (!trigger) return;
    const item = trigger.closest('.syllabus-accordion__item') as HTMLElement | null;
    if (!item) return;
    const isOpen = item.classList.toggle('syllabus-accordion__item--open');
    trigger.setAttribute('aria-expanded', String(isOpen));
  });
</script>

<style is:global>
  /* ==================================================================
     COURSE PAGE — PREMIUM DESIGN SYSTEM
     Archetype: Soft Structuralism + Asymmetrical Bento
     Typography: Geist (via CDN) — clean grotesk with editorial weight
     ================================================================== */

  @import url('https://fonts.cdnfonts.com/css/geist');

  :root {
    --course-surface: #f8f9fb;
    --course-card: #ffffff;
    --course-ink: #0f1117;
    --course-muted: #6b7280;
    --course-line: rgba(0, 0, 0, 0.06);
    --course-accent: #2563eb;
    --course-accent-glow: rgba(37, 99, 235, 0.08);
    --course-radius-lg: 1.5rem;
    --course-radius-md: 1rem;
    --course-radius-sm: 0.5rem;
    --course-transition: 0.5s cubic-bezier(0.32, 0.72, 0, 1);
    --course-transition-fast: 0.3s cubic-bezier(0.32, 0.72, 0, 1);
  }

  /* ---- Staggered reveal ---- */
  .reveal-up {
    opacity: 1;
    transform: translateY(0);
    filter: none;
    transition: opacity 0.9s cubic-bezier(0.32, 0.72, 0, 1),
                transform 0.9s cubic-bezier(0.32, 0.72, 0, 1),
                filter 0.7s cubic-bezier(0.32, 0.72, 0, 1);
    transition-delay: var(--reveal-delay, 0s);
  }
  .js .reveal-up {
    opacity: 0;
    transform: translateY(2rem);
    filter: blur(4px);
  }
  .reveal-up--visible {
    opacity: 1;
    transform: translateY(0);
    filter: blur(0);
  }
  @media (prefers-reduced-motion: reduce) {
    .reveal-up {
      opacity: 1;
      transform: none;
      filter: none;
      transition: none;
    }
  }

  /* ---- Cinematic Hero ---- */
  .course-hero {
    position: relative;
    min-height: min(85dvh, 700px);
    display: flex;
    align-items: flex-end;
    background-color: #0f1117;
    overflow: hidden;
  }
  /* Blurred backdrop — the background image lives on its own layer so the
     blur applies to the image itself. This works in every browser, unlike
     `backdrop-filter` on the overlay, which Safari does not apply to the
     parent element's background. The scale hides edge bleed from the blur. */
  .course-hero::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: var(--hero-image, none);
    background-size: cover;
    background-position: center 30%;
    filter: blur(14px) saturate(1.05);
    transform: scale(1.12);
    z-index: 0;
  }
  .course-hero__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg,
      rgba(15, 17, 23, 0.25) 0%,
      rgba(15, 17, 23, 0.55) 40%,
      rgba(15, 17, 23, 0.92) 100%
    );
    z-index: 1;
  }
  .course-hero::after {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(ellipse at 30% 20%, rgba(37, 99, 235, 0.12) 0%, transparent 60%);
    z-index: 0;
  }
  .course-hero__inner {
    position: relative;
    z-index: 2;
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 6rem 2rem 3rem;
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
  }
  .course-hero__content {
    max-width: 680px;
  }
  .course-hero__eyebrow {
    display: inline-block;
    padding: 0.35rem 0.85rem;
    margin-bottom: 1.25rem;
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 0.68rem;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.85);
    background: rgba(20, 24, 32, 0.72);
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 100px;
    -webkit-backdrop-filter: blur(12px);
    backdrop-filter: blur(12px);
  }
  .course-hero__title {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: clamp(2.25rem, 5vw, 3.75rem);
    font-weight: 600;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: #ffffff;
    margin: 0 0 1rem;
  }
  .course-hero__subtitle {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.1rem;
    line-height: 1.55;
    color: rgba(255, 255, 255, 0.7);
    max-width: 520px;
    margin: 0 0 1.75rem;
  }
  .course-hero__actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    align-items: center;
  }

  /* Hero stats */
  .course-hero__stats {
    display: flex;
    gap: 2rem;
    padding: 1.25rem 1.75rem;
    background: rgba(20, 24, 32, 0.74);
    -webkit-backdrop-filter: blur(24px);
    backdrop-filter: blur(24px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--course-radius-lg);
    width: fit-content;
  }
  .course-hero__stat {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
  .course-hero__stat-value {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.25rem;
    font-weight: 600;
    color: #ffffff;
  }
  .course-hero__stat-label {
    font-size: 0.72rem;
    color: rgba(255, 255, 255, 0.55);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  /* ---- Main content grid ---- */
  .course-main {
    padding: 4rem 2rem 2rem;
    background: var(--course-surface);
  }
  .course-main__grid {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 340px;
    gap: 2rem;
    align-items: start;
  }
  .course-main__content {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }
  .course-main__sidebar {
    position: sticky;
    top: 1.5rem;
  }

  @media (max-width: 960px) {
    .course-main__grid {
      grid-template-columns: 1fr;
    }
    .course-main__sidebar {
      position: static;
      order: -1;
    }
  }
  @media (max-width: 640px) {
    .course-hero__inner { padding: 4rem 1rem 2rem; }
    .course-hero__stats { flex-wrap: wrap; gap: 1.25rem; }
    .course-main { padding: 2rem 1rem 1rem; }
  }

  /* ---- Double-Bezel Card ---- */
  .card-premium {
    --shell-bg: rgba(0, 0, 0, 0.03);
    --shell-border: rgba(0, 0, 0, 0.04);
    --core-bg: #ffffff;
  }
  .card-premium__shell {
    padding: 1.5px;
    background: var(--shell-bg);
    border: 1px solid var(--shell-border);
    border-radius: calc(var(--course-radius-lg) + 1.5px);
    box-shadow: 0 1px 2px rgba(0, 0, 0, 0.03);
  }
  .card-premium__core {
    background: var(--core-bg);
    border-radius: var(--course-radius-lg);
    padding: 2rem;
    box-shadow: inset 0 1px 1px rgba(255, 255, 255, 0.6);
  }
  .card-premium--sticky {
    --shell-bg: rgba(37, 99, 235, 0.06);
    --shell-border: rgba(37, 99, 235, 0.1);
  }
  .card-premium__title {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.75rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--course-ink);
    margin: 0 0 0.75rem;
  }
  .card-premium__body {
    font-size: 0.95rem;
    line-height: 1.65;
    color: var(--course-muted);
    margin: 0;
  }
  .card-premium__body-sm {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--course-muted);
    margin: 0.5rem 0 0;
  }
  .card-premium__hero-image {
    width: 100%;
    aspect-ratio: 16/7;
    object-fit: cover;
    border-radius: var(--course-radius-md);
    margin-top: 1.5rem;
  }
  .card-premium__section-title {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.15rem;
    font-weight: 600;
    color: var(--course-ink);
    margin: 0 0 1rem;
  }
  .card-premium__block {
    padding: 1.25rem 0;
  }
  .card-premium__block + .card-premium__block {
    border-top: 1px solid var(--course-line);
  }
  .card-premium__block-title {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--course-ink);
    margin: 0 0 0.5rem;
  }
  .card-premium__blocks { display: flex; flex-direction: column; }

  /* Objectives grid */
  .card-premium__objectives-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 0.75rem;
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .card-premium__objectives-grid li {
    display: flex;
    gap: 0.6rem;
    font-size: 0.9rem;
    color: var(--course-muted);
    line-height: 1.5;
  }
  .check-icon { color: var(--course-accent); font-weight: 600; flex-shrink: 0; }
  @media (max-width: 640px) {
    .card-premium__objectives-grid { grid-template-columns: 1fr; }
  }

  /* Generic list */
  .card-premium__list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
  }
  .card-premium__list li {
    display: flex;
    gap: 0.6rem;
    font-size: 0.9rem;
    color: var(--course-muted);
    line-height: 1.5;
  }
  .card-premium__list li span:first-child {
    color: var(--course-accent);
    flex-shrink: 0;
  }
  .card-premium__video-links {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  /* ---- Magnetic Button (with inner icon) ---- */
  .btn-magnetic {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.8rem 1.5rem;
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 0.9rem;
    font-weight: 500;
    color: #ffffff;
    background: var(--course-accent);
    border: none;
    border-radius: 100px;
    cursor: pointer;
    text-decoration: none;
    transition: transform var(--course-transition-fast),
                box-shadow var(--course-transition-fast);
    box-shadow: 0 2px 8px rgba(37, 99, 235, 0.25);
  }
  .btn-magnetic:hover {
    transform: scale(1.03);
    box-shadow: 0 4px 16px rgba(37, 99, 235, 0.35);
  }
  .btn-magnetic:active {
    transform: scale(0.98);
  }
  .btn-magnetic--full {
    width: 100%;
    justify-content: center;
  }
  .btn-magnetic__icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.15);
    transition: transform var(--course-transition-fast);
  }
  .btn-magnetic:hover .btn-magnetic__icon {
    transform: translateX(2px) translateY(-1px) scale(1.05);
  }

  /* Ghost button */
  .btn-ghost {
    display: inline-flex;
    align-items: center;
    padding: 0.7rem 1.25rem;
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 0.88rem;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.8);
    background: transparent;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 100px;
    text-decoration: none;
    transition: all var(--course-transition-fast);
    backdrop-filter: blur(8px);
  }
  .btn-ghost:hover {
    color: #ffffff;
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.35);
  }
  .btn-ghost-sm {
    display: inline-flex;
    align-items: center;
    padding: 0.45rem 0.9rem;
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 0.8rem;
    font-weight: 500;
    color: var(--course-muted);
    background: transparent;
    border: 1px solid var(--course-line);
    border-radius: 100px;
    text-decoration: none;
    transition: all var(--course-transition-fast);
  }
  .btn-ghost-sm:hover {
    color: var(--course-ink);
    border-color: rgba(0, 0, 0, 0.15);
    background: rgba(0, 0, 0, 0.03);
  }

  /* ---- Sidebar ---- */
  .sidebar-price {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    margin-bottom: 1.25rem;
    flex-wrap: wrap;
  }
  .sidebar-price__value {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 2rem;
    font-weight: 600;
    color: var(--course-ink);
  }
  .sidebar-price__original {
    font-size: 1rem;
    color: var(--course-muted);
    text-decoration: line-through;
    opacity: 0.6;
  }
  .sidebar-price__discount {
    font-size: 0.75rem;
    font-weight: 600;
    color: #ffffff;
    background: #ef4444;
    padding: 0.15rem 0.5rem;
    border-radius: 100px;
  }
  .sidebar-facts {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1.25rem 0;
    border-top: 1px solid var(--course-line);
    border-bottom: 1px solid var(--course-line);
    margin-bottom: 0.5rem;
  }
  .sidebar-facts__row {
    display: flex;
    justify-content: space-between;
    font-size: 0.85rem;
  }
  .sidebar-facts__row dt { color: var(--course-muted); font-weight: 400; }
  .sidebar-facts__row dd { color: var(--course-ink); font-weight: 500; }
  .sidebar-facts__rating { color: #f59e0b; }
  .sidebar-discount-note {
    font-size: 0.75rem;
    color: var(--course-muted);
    text-align: center;
    margin: 0.75rem 0 0;
  }
  .sidebar-cert-badge {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-top: 1rem;
    padding: 0.75rem 0.85rem;
    background: rgba(34, 197, 94, 0.06);
    border: 1px solid rgba(34, 197, 94, 0.12);
    border-radius: var(--course-radius-md);
    font-size: 0.8rem;
    font-weight: 500;
    color: #16a34a;
  }

  /* ---- Syllabus Accordion ---- */
  .syllabus-section {
    padding: 4rem 2rem;
    background: var(--course-surface);
  }
  .syllabus-section__inner {
    max-width: 960px;
    margin: 0 auto;
  }
  .syllabus-section__header {
    text-align: center;
    margin-bottom: 2.5rem;
  }
  .syllabus-section__title {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 2rem;
    font-weight: 600;
    letter-spacing: -0.01em;
    color: var(--course-ink);
    margin: 0.5rem 0 0.25rem;
  }
  .syllabus-section__subtitle {
    font-size: 0.9rem;
    color: var(--course-muted);
    margin: 0;
  }

  /* Accordion */
  .syllabus-accordion {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }
  .syllabus-accordion__item {
    background: #ffffff;
    border: 1px solid var(--course-line);
    border-radius: var(--course-radius-md);
    overflow: hidden;
    transition: border-color var(--course-transition-fast),
                box-shadow var(--course-transition-fast);
  }
  .syllabus-accordion__item:hover {
    border-color: rgba(0, 0, 0, 0.1);
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.04);
  }
  .syllabus-accordion__item--open {
    border-color: rgba(37, 99, 235, 0.15);
    box-shadow: 0 2px 16px rgba(37, 99, 235, 0.06);
  }
  .syllabus-accordion__trigger {
    display: flex;
    align-items: center;
    gap: 1rem;
    width: 100%;
    padding: 1.15rem 1.5rem;
    background: transparent;
    border: none;
    cursor: pointer;
    text-align: left;
    font-family: 'Geist', system-ui, sans-serif;
  }
  .syllabus-accordion__index {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--course-accent);
    min-width: 2rem;
    font-variant-numeric: tabular-nums;
  }
  .syllabus-accordion__label {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
  .syllabus-accordion__module-title {
    font-size: 0.95rem;
    font-weight: 500;
    color: var(--course-ink);
  }
  .syllabus-accordion__module-desc {
    font-size: 0.78rem;
    color: var(--course-muted);
  }
  .syllabus-accordion__meta {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    flex-shrink: 0;
  }
  .syllabus-accordion__lesson-count {
    font-size: 0.75rem;
    color: var(--course-muted);
    white-space: nowrap;
  }
  .syllabus-accordion__chevron {
    display: flex;
    transition: transform var(--course-transition-fast);
  }
  .syllabus-accordion__item--open .syllabus-accordion__chevron {
    transform: rotate(180deg);
  }
  .syllabus-accordion__panel {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows var(--course-transition);
  }
  .syllabus-accordion__item:first-child .syllabus-accordion__panel,
  .syllabus-accordion__item--open .syllabus-accordion__panel {
    grid-template-rows: 1fr;
  }
  .syllabus-accordion__panel > * {
    overflow: hidden;
  }
  .syllabus-accordion__lessons {
    list-style: none;
    padding: 0 1.5rem 1.25rem;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .syllabus-accordion__lesson {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.55rem 0.75rem;
    border-radius: var(--course-radius-sm);
    transition: background var(--course-transition-fast);
    font-size: 0.85rem;
  }
  .syllabus-accordion__lesson:hover {
    background: rgba(0, 0, 0, 0.02);
  }
  .syllabus-accordion__lesson-icon { flex-shrink: 0; display: flex; }
  .syllabus-accordion__lesson-title {
    flex: 1;
    color: var(--course-ink);
    font-weight: 450;
  }
  .syllabus-accordion__lesson-duration {
    font-size: 0.75rem;
    color: var(--course-muted);
    white-space: nowrap;
  }
  .syllabus-accordion__lesson-badge {
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: #16a34a;
    background: rgba(34, 197, 94, 0.08);
    padding: 0.15rem 0.5rem;
    border-radius: 100px;
  }

  /* ---- Instructor Card ---- */
  .instructor-section {
    padding: 4rem 2rem;
    background: var(--course-surface);
  }
  .instructor-section__inner {
    max-width: 760px;
    margin: 0 auto;
  }
  .instructor-card {
    display: flex;
    gap: 1.75rem;
    align-items: flex-start;
  }
  .instructor-card__avatar {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    object-fit: cover;
    flex-shrink: 0;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  }
  .instructor-card__avatar-placeholder {
    width: 100px;
    height: 100px;
    border-radius: 50%;
    background: var(--course-accent-glow);
    color: var(--course-accent);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 2.2rem;
    font-weight: 600;
    font-family: 'Geist', system-ui, sans-serif;
    flex-shrink: 0;
  }
  .instructor-card__info { flex: 1; }
  .instructor-card__name {
    font-family: 'Geist', system-ui, sans-serif;
    font-size: 1.35rem;
    font-weight: 600;
    color: var(--course-ink);
    margin: 0.35rem 0 0.25rem;
  }
  .instructor-card__title {
    font-size: 0.85rem;
    color: var(--course-muted);
    margin: 0 0 0.75rem;
  }
  .instructor-card__bio {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--course-muted);
    margin: 0 0 1rem;
  }
  .instructor-card__links {
    display: flex;
    gap: 0.5rem;
  }
  @media (max-width: 480px) {
    .instructor-card { flex-direction: column; align-items: center; text-align: center; }
    .instructor-card__links { justify-content: center; }
  }
</style>
