---
import Layout from '@/layouts/Layout.astro';
import Hero from '@/components/blocks/Hero.astro';
import Timeline from '@/components/blocks/Timeline.astro';
import CTA from '@/components/blocks/CTA.astro';
import Toast from '@/components/ui/Toast.astro';
import { cachedSiteSettings, cachedNavigation, cachedPageData } from '@/lib/api';
import type { SiteSettings, NavigationData, PageData } from '@/lib/api';

let siteSettings: SiteSettings | undefined;
let navData: NavigationData | undefined;
let pageData: PageData | undefined;
try {
  [siteSettings, navData, pageData] = await Promise.all([
    cachedSiteSettings(), cachedNavigation(), cachedPageData('services'),
  ]);
} catch { /* use fallbacks */ }

const pageHero = pageData?.hero;
const pageCta = pageData?.cta;
const pageServices = (pageData?.services as any[] | undefined);
const pageProcess = (pageData?.process as any[] | undefined);

const serviceItems = (pageServices || []).map((s: any) => ({
  icon: String(s.icon || 'M12 2l8 4v6c0 5-3.5 8-8 10-4.5-2-8-5-8-10V6l8-4z'),
  title: String(s.title || ''),
  description: String(s.description || ''),
  deliverables: (s.deliverables || []) as string[],
  cta_label: String(s.cta_label || ''),
  cta_href: String(s.cta_href || ''),
}));

const processSteps = (pageProcess || []).map((p: any) => ({
  date: String(p.date || ''),
  title: String(p.title || ''),
  description: String(p.description || ''),
  icon: String(p.icon || ''),
}));
---


<Layout
  title={pageData?.title || 'Services'}
  description={pageData?.search_description || 'CTC Research services — clinical research, evidence synthesis, and health innovation programs.'}
  navItems={navData?.nav_items}
  siteSettings={siteSettings}
  pageData={pageData}
  pageSlug="services"
>
  <Hero
    eyebrow={pageHero?.badge || 'services · evidence support'}
    heading={pageHero?.title || 'Research services'}
    accent='for clinical teams'
    intro={pageHero?.subtitle || 'Protocol development, evidence synthesis, medical writing, and rigorous research methods for teams that need clear methods and publishable results.'}
    primaryCta={pageHero?.primary_cta ? { label: pageHero.primary_cta.label, href: pageHero.primary_cta.href } : { label: 'Discuss a study', href: '/contact/' }}
    secondaryCta={pageHero?.secondary_cta ? { label: pageHero.secondary_cta.label, href: pageHero.secondary_cta.href } : { label: 'Explore courses', href: '/courses/' }}
    variant="contact"
  />

  {serviceItems.length ? (
  <section class='doc-sheet bg-fu-paper py-20 md:py-24'>
    <div class='container-fusion'>
      <div class='mx-auto mb-14 max-w-2xl text-center'>
        <p class='tag-marker mb-4'>what we do</p>
        <h2 class='mb-4 font-display text-3xl font-bold tracking-tight text-fu-ink md:text-4xl'>A clear route from question to evidence</h2>
        <p class='text-fu-muted'>Each service keeps the research question, analysis plan, and final communication aligned from the first protocol review to the submitted manuscript.</p>
      </div>
      <div class='grid grid-cols-1 gap-5 md:grid-cols-2 lg:grid-cols-3'>
        {serviceItems.map((service, i) => (
          <div class='card reveal flex flex-col gap-4 p-6' x-data x-intersect="$el.classList.add('reveal-visible')" style={`transition-delay: ${i * 80}ms`}>
            <div class='mb-1 inline-flex h-10 w-10 items-center justify-center rounded-md border border-fu-line bg-fu-card text-fu-link'>
              <svg class='h-5 w-5' fill='none' viewBox='0 0 24 24' stroke='currentColor' stroke-width='1.8' aria-hidden='true'>
                <path stroke-linecap='round' stroke-linejoin='round' d={service.icon} />
              </svg>
            </div>
            <h3 class='font-display text-[1.05rem] font-semibold text-fu-ink'>{service.title}</h3>
            {service.description && <p class='text-sm leading-relaxed text-fu-muted'>{service.description}</p>}
            {service.deliverables.length > 0 && (
              <ul class='flex flex-col gap-1.5'>
                {service.deliverables.map((d: string) => (
                  <li class='flex items-baseline gap-2 text-sm text-fu-muted'><span class='flex-shrink-0 text-fu-link'>•</span><span>{d}</span></li>
                ))}
              </ul>
            )}
            {service.cta_label && (
              <a href={service.cta_href || '/contact/'} class='btn-secondary mt-auto w-fit !py-2 text-sm'>{service.cta_label}</a>
            )}
          </div>
        ))}
      </div>
    </div>
  </section>
  ) : null}

  {processSteps.length ? (
    <Timeline
      eyebrow='research workflow'
      title='From protocol to publication'
      description='A transparent workflow keeps scope, evidence quality, and reporting decisions visible to the whole study team.'
      events={processSteps}
    />
  ) : null}

  <CTA
    heading={pageCta?.title || 'Advance health through research'}
    intro={pageCta?.subtitle || 'Partner with CTC Research on clinical research, evidence synthesis, and health innovation.'}
    ctaLabel={pageCta?.primary_cta?.label || 'Contact us'}
    ctaHref={pageCta?.primary_cta?.href || '/contact/'}
  />
  <Toast />
</Layout>
