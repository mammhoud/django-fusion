{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "precis-landing-assets",
  "root": "projects/precis/precis-landing",
  "projectType": "library",
  "tags": ["product:precis-landing", "type:assets", "stack:webpack"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["assets", "^production"],
      "cache": true,
      "options": { "command": "npm run build", "cwd": "projects/precis/precis-landing" }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run dev", "cwd": "projects/precis/precis-landing" }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": { "command": "npm run clean", "cwd": "projects/precis/precis-landing" }
    }
  }
}
