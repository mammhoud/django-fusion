# POS Full Server dependencies
# Synchronised with pyproject.toml — prefer `uv sync`; this file is a pip fallback.

django>=5.2.8,<6.0
django-unfold>=0.80,<1.0
asgiref>=3.8
sqlparse>=0.4
httpx>=0.27
pydantic>=2.0
Jinja2>=3.1
django-tables2>=2.7
wagtail>=7.0,<8.0

# django-bolt + django-fusion (installed from the local libs via [tool.uv.sources])
# django-bolt[pydantic]
# django-fusion

# API layer (django-fusion encoder/decoder + Ninja contract)
django-ninja>=1.0
django-ninja-extra>=0.19
ninja-schema>=0.14
django-htmx>=1.19

# QR Menu
qrcode>=7.4,<9.0

# ASGI server (replaces Robyn) + Channels WebSockets
daphne>=4.0
channels>=4.0

# django-bolt JWT auth
PyJWT>=2.8

# Test dependencies
pytest>=7.0
pytest-asyncio>=0.23
