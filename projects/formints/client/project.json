{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "pos-client",
  "root": "projects/formints/formint-client",
  "targets": {
    "build": { "executor": "nx:run-commands", "options": { "command": "npm run build", "cwd": "projects/formints/formint-client" } },
    "check": { "executor": "nx:run-commands", "options": { "command": "npm run lint", "cwd": "projects/formints/formint-client" } }
  }
}
