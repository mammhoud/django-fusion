# POS Full Sidecar dependencies
# Synchronised with pyproject.toml — use `uv sync` or `pip install -r requirements.txt`

django>=4.2,<5.1
django-unfold>=0.50,<0.60
channels>=4.0
daphne>=4.0
websockets>=12.0
asgiref>=3.8
httpx>=0.27
pydantic>=2.0

# django-bolt (high-performance Rust-backed API) + django-fusion (dual-mode contract)
django-bolt[pydantic]
django-fusion

# Test dependencies
pytest>=7.0
pytest-asyncio>=0.23
