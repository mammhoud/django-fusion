// Tailwind CSS v4 is processed by the @tailwindcss/vite plugin (see
// astro.config.mjs) — this file exists only to keep tooling that auto-detects
// a postcss config from double-processing CSS. Keep it empty.
export default {
  plugins: {},
}
