# ============================================================
# Main Orchestration — Website Services
# ============================================================
# Infrastructure Services (Traefik, PostgreSQL, Redis) are defined 
# in the root docker-compose.yml and automatically connected via shared networks.
#
# This file includes the active application containers that need rebuilding:
#   - Precis LMS and Landing-Fusion
# ============================================================

include:
  - path: ./precis/precis-lms/docker-compose.yml
  - path: ./precis/precis-landing/docker-compose.yml
