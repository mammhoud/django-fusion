<!--
  HTMX bootstrap — loads the HTMX runtime and event listeners globally.
  The footer newsletter and live content fragments can therefore work on any
  page without duplicating the runtime or adding page-specific bootstraps.
-->

<script>
  import '../lib/htmx-bootstrap';
</script>
