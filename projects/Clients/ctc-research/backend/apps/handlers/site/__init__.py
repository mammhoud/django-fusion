"""Site-level views for the handlers app.

Submodules (``blog``, ``tags``, ``asset_health``, ``media_health``) are
imported directly by their consumers; this package marker intentionally
exports nothing to avoid stale shim imports.
"""
