"""Per-project environment overrides for precis-landing.

Precis Landing keeps a standalone Django + Wagtail settings module
(``backend/settings.py``) and resolves settings through ``os.environ``. This
directory documents the site-level identity contract and keeps the ``Env``
layout in parity with the other Precis sites (lms-ctc, main).
"""
