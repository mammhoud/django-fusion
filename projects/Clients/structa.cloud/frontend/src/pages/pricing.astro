---
/**
 * Pricing — minimal shell. The backend fragment (/fragment/pages/pricing/) is
 * the single source of truth for the hero, per-product pricing tabs, faq and
 * cta (get_product_pricing renders the tabbed editions from Wagtail).
 */
import BackendPage from '@/components/BackendPage.astro';
---

<BackendPage slug="pricing" />
