"""Canonical shared configuration package for all workspace sites.

Site-specific Django settings import this package as ``configs``. Runtime
component configuration remains in ``django_fusion.config``; this package
owns workspace/site settings and environment profiles.
"""
