# Django-Material Analysis & django-fusion Mapping

Status: Planned

Summary: A comprehensive analysis of the `django-material` library's URL patterns, view classes, and template conventions, mapped to the equivalent patterns in `django-fusion`. This spec will document every route and component from the django-material `demo/urls.py` and show the 1:1 equivalent in django-fusion's `comp.routes` and `comp.generic` modules.

## What Will Replace This Page
- Full URL mapping table with columns: django-material URL name, View/handler class, django-fusion equivalent (canonical import path), Notes
- Side-by-side code examples showing `AtlasApp` / `ModelViewset` migration patterns
- Gap analysis identifying django-material features with no django-fusion equivalent
