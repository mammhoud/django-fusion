[project]
name = "crm"
version = "1.0.3"
description = "CRM website — part of Structa Cloud workspace"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["www", "plugins"]
