# ============================================================
# Applications – Main Orchestration
# ============================================================
# Includes the active project Compose files. Legacy ctc/lms/portfolio and
# cypercloud Compose paths were removed when those projects were consolidated.
# ============================================================

include:
  - ./../../projects/precis/docker-compose.yml
  - ./../../projects/landing-fusion/docker-compose.yml

# Networks and volumes are defined in the root docker-compose.yml
# (common / traefik-net / internal + postgres_data / redis_data /
# per-site static & media volumes).
