# ============================================================
# Applications – Main Orchestration
# ============================================================
# Includes individual website compose files (current project
# layout uses `core/`, not the legacy `applications/`).
# ============================================================

include:
  - ./../core/ctc-research/docker-compose.yml
  - ./../core/lms-demo/docker-compose.yml
  - ./../core/VResume/docker-compose.yml
  # - ./../core/crm/docker-compose.yml  # CRM directory empty, not available
  - ./../core/tinker/docker-compose.yml

# Networks and volumes are defined in the root docker-compose.yml
# (common / traefik-net / internal + postgres_data / redis_data /
# per-site static & media volumes).
