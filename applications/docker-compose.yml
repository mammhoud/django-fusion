# ============================================================
# Main Orchestration — Website Services
# ============================================================
# Traefik  → managed by Coolify (coolify-proxy, traefik:v3.6)
# Postgres → managed by Coolify (coolify-db, postgres:15-alpine)
# Redis    → managed by Coolify (coolify-redis, redis:7-alpine)
#
# This file only manages application containers that need rebuilding:
#   - ctc-research-website, lms-web, vresume-web
#   - structa-docs (Docsify)
# ============================================================

include:
  - path: compose/docker-compose.applications.yml
  - path: docker-compose.infra.yml
