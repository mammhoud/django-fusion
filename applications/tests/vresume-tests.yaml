site: vresume
aliases: [resume, VResume, vresume.structa.cloud]
base_url: http://vresume.local
port: 5072
commands:
  check: python manage.py --site vresume check
  build: npm --prefix assets run build -- --site vresume
routes:
  - path: /health/
    status: 200
  - path: /cms/
    status: 302
