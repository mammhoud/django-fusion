# =============================================================================
# cms-fusion — Traefik router config
# =============================================================================
# Local development host: cms-fusion.localhost
# Adjust host rules for production domains.
# =============================================================================

http:
  routers:
    cms-fusion-site-http:
      rule: 'Host(`cms-fusion.localhost`) || Host(`cms-fusion.com`) || Host(`www.cms-fusion.com`)'
      entryPoints: [web]
      middlewares: [redirect-to-https]
      service: cms-fusion-frontend-service
      priority: 10

    cms-fusion-site-https:
      rule: 'Host(`cms-fusion.localhost`) || Host(`cms-fusion.com`) || Host(`www.cms-fusion.com`)'
      entryPoints: [web-secure]
      middlewares: [compress, csrf-headers, security-headers]
      service: cms-fusion-frontend-service
      tls:
        certResolver: letsencrypt-http
      priority: 10

    cms-fusion-api:
      rule: '(Host(`cms-fusion.localhost`) || Host(`cms-fusion.com`) || Host(`www.cms-fusion.com`)) && PathPrefix(`/api`)'
      entryPoints: [web, web-secure]
      middlewares: [compress, csrf-headers, security-headers]
      service: cms-fusion-backend-service
      tls:
        certResolver: letsencrypt-http
      priority: 100

    cms-fusion-media:
      rule: '(Host(`cms-fusion.localhost`) || Host(`cms-fusion.com`) || Host(`www.cms-fusion.com`)) && (PathPrefix(`/static/`) || PathPrefix(`/media/`) || PathPrefix(`/sites/`))'
      entryPoints: [web, web-secure]
      middlewares: [compress, security-headers]
      service: cms-fusion-media-service
      priority: 110

  services:
    cms-fusion-frontend-service:
      loadBalancer:
        servers:
          - url: 'http://cms-fusion-frontend:3002'
        healthCheck:
          path: /
          interval: 30s
          timeout: 10s

    cms-fusion-backend-service:
      loadBalancer:
        servers:
          - url: 'http://cms-fusion-backend:5075'
        healthCheck:
          path: /health/
          interval: 30s
          timeout: 10s

    cms-fusion-media-service:
      loadBalancer:
        servers:
          - url: 'http://shared-media:80'
