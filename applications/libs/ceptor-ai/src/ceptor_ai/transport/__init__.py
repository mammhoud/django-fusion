"""Transport layer — HTTP middlewares, signals, and request handlers.

Sub-packages
------------
transport.middlewares   Privacy-consent and other request/response middleware.
transport.signals       Django signal bindings (user lifecycle, invitations, notifications).
transport.handlers      Wagtail hooks and model stubs for the HTTP handler layer.
"""
