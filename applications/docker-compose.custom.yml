# docker-compose.custom.yml
# Custom overrides for Coolify deployments.
# This file is merged after docker-compose.yml and docker-compose.prod.yml, allowing you to
# specify environment-specific settings, additional services, or resource tweaks.

services:
  # Example: add a sidecar container or adjust resources
  # ctc-research-website:
  #   environment:
  #     - DEBUG=True
  #   deploy:
  #     resources:
  #       limits:
  #         cpus: "0.3"
  #         memory: 256M

# Add any custom service definitions or overrides here.
