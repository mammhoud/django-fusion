# Formints JS/TS Client Bundle — Local Completion Record

**Canonical package:** `projects/formints/packages/formints-client/`
**Consumer:** `projects/formints/formint-cloud/frontend/`

**Status:** Complete. `@formints/client` is a framework-agnostic, fetch-only,
dependency-free TypeScript package (ESM + `.d.ts`) with core client + `ApiError`,
currency, tax, export, and monitor modules, barrel/subpath exports, and Cloud
frontend consumption of the monitor module.

## Remaining work

- [ ] Package publishing / registry release / git commit — repository-owner
  actions, prepared locally only.
