"""Tests for django_fusion.fragments.analyzer sub-modules.

Each module under this subpackage focuses on one analyzer component
(parser, scanner, view, ...). The flat `tests/test_analyzer_*.py` files
that pre-date this layout have been folded in here; the parser suite
currently lives in `test_parser.py`.
"""
