[project]
name = "django-osoul-stub"
version = "0.1.0"
description = "Stub/mock package for django-osoul to handle missing dependencies"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/django_osoul"]
